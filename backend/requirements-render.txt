# Render-optimized requirements with pre-built wheels
# Use this if main requirements.txt still has issues

# Build dependencies - install first
numpy>=1.24.0,<2.0.0

# Core API - using versions with pre-built wheels
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
sqlalchemy==2.0.23
pydantic==2.5.0
python-dotenv==1.0.0
requests==2.31.0

# LangChain - using compatible versions
langchain==0.1.0
langchain-community==0.0.10
langchain-text-splitters==0.0.1
langchain-huggingface==0.0.1

# Vector Database - using version with pre-built wheels
chromadb==0.4.22

# Embeddings
sentence-transformers==2.2.2

# Document Processing
PyPDF2==3.0.1
python-docx==1.1.0
pypdf==3.17.1

# Image Processing
Pillow>=10.2.0,<11.0.0

# ML/AI
huggingface-hub==0.19.4

# OCR dependencies - commented out to avoid build issues
# pytesseract==0.3.10  # Requires system Tesseract
# easyocr==1.7.0  # Large download, may timeout

# Async file operations
aiofiles==23.2.1

