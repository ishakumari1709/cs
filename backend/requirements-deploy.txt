# Deployment-optimized requirements
# This file uses versions with better pre-built wheel support

# Build dependencies - install first
numpy>=1.24.0

# Core API
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
sqlalchemy==2.0.23
pydantic==2.5.0
python-dotenv==1.0.0
requests==2.31.0

# LangChain - using compatible versions
langchain==0.1.0
langchain-community==0.0.10
langchain-text-splitters==0.0.1
langchain-huggingface==0.0.1

# Vector Database - newer version with better wheel support
chromadb>=0.4.22

# Embeddings
sentence-transformers==2.2.2

# Document Processing
PyPDF2==3.0.1
python-docx==1.1.0
pypdf==3.17.1

# Image Processing
Pillow>=10.2.0

# ML/AI
huggingface-hub==0.19.4

# OCR dependencies - EasyOCR commented out as it's large and may cause build issues
# Option 1: Tesseract (requires system installation - won't work on Render)
# pytesseract==0.3.10

# Option 2: EasyOCR (large download, may cause timeout on free tier)
# easyocr==1.7.0

# Async file operations
aiofiles==23.2.1

